AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31

Description: Serverless handlers (Cognito + DynamoDB + HTTP API + Lambda) for the React app

Globals:
  Function:
    Runtime: nodejs24.x
    Timeout: 10
    MemorySize: 128

Parameters:
  CognitoUserPoolName:
    Type: String
    Default: fitness-app-user-pool

  CognitoUserPoolClientName:
    Type: String
    Default: fitness-app-client-user-pool

Resources:

# ----------------------- Identity (Cognito) --------------------------------
  MyCognitoUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: !Ref CognitoUserPoolName
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireUppercase: true
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: true
      UsernameAttributes:
        - email
      AutoVerifiedAttributes:
        - email
      Schema:
        - AttributeDataType: String
          Name: email
          Required: true

  MyCognitoUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      UserPoolId: !Ref MyCognitoUserPool
      ClientName: !Ref CognitoUserPoolClientName
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_USER_SRP_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH

  AppDataTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: AppData
      BillingMode:
        PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH

  WorkoutDataTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: WorkoutData
      BillingMode:
        PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
        - AttributeName: workoutId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
        - AttributeName: workoutId
          KeyType: RANGE

# ----------------------- API --------------------------------
  MyHttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      CorsConfiguration:
        AllowOrigins:
          - http://localhost:5173
        AllowHeaders:
          - Authorization
          - Content-Type
        AllowMethods:
          - GET
          - PUT
          - POST
          - DELETE
          - OPTIONS
      Auth:
        Authorizers:
          CognitoJwtAuthorizer:
            IdentitySource: "$request.header.Authorization"
            JwtConfiguration:
              issuer: !Sub "https://cognito-idp.${AWS::Region}.amazonaws.com/${MyCognitoUserPool}"
              audience:
                - !Ref MyCognitoUserPoolClient
        DefaultAuthorizer: CognitoJwtAuthorizer

# ----------------------- Profile Functions --------------------------------
  GetProfileFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: handlers/profile/getProfile.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref AppDataTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref AppDataTable
      Events:
        GetProfile:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /profile
            Method: get

  SaveProfileFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: handlers/profile/saveProfile.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref AppDataTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref AppDataTable
      Events:
        SaveProfile:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /profile
            Method: put

# ----------------------- Workout Functions --------------------------------

  GetWorkoutFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: handlers/workout/getWorkout.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref WorkoutDataTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref WorkoutDataTable
      Events:
        GetWorkout:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /workouts/{workoutId}
            Method: get

  CreateWorkoutFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: handlers/workout/createWorkout.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref WorkoutDataTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref WorkoutDataTable
      Events:
        CreateWorkout:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /workouts
            Method: post

  ListWorkoutsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: handlers/workout/listWorkouts.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref WorkoutDataTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref WorkoutDataTable
      Events:
        CreateWorkout:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /workouts
            Method: get

  RemoveWorkoutFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: handlers/workout/removeWorkout.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref WorkoutDataTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref WorkoutDataTable
      Events:
        RemoveWorkout:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /workouts/{workoutId}
            Method: delete

Outputs:
  UserPoolId:
    Description: Cognito User Pool ID
    Value: !Ref MyCognitoUserPool

  UserPoolClientId:
    Description: Cognito App Client ID
    Value: !Ref MyCognitoUserPoolClient

  ApiUrl:
    Description: Base URL of the HTTP API for frontend calls.
    Value: !Sub "https://${MyHttpApi}.execute-api.${AWS::Region}.amazonaws.com"

  Region:
    Description: AWS Region stack deployed to
    Value: !Ref AWS::Region